@{

    if (Session["Username"] != null && Session["Username"].ToString() !="") {
        Session["Username"] = null;
        Response.Redirect("~/");
       
    } else {
        Response.Redirect("~/");
    }
}
